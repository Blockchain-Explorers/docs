{
  "name": "mkdocs-gh-pages-template",
  "version": "1.0",
  "scripts": {
    "dev": "python3 -m pip install -r requirements.txt && python3 -m mkdocs serve",
    "build": "python3 -m pip install -r requirements.txt && python3 -m mkdocs build --verbose --clean",
    "prepare": "husky install",
    "predeploy": "npm run build",
    "deploy": "python3 -m mkdocs gh-deploy"
  },
  "devDependencies": {
    "husky": "^9.1.7"
  }
}
